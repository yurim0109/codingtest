SELECT O.ANIMAL_ID, O.ANIMAL_TYPE, O.NAME
FROM ANIMAL_INS I LEFT OUTER JOIN ANIMAL_OUTS O USING (ANIMAL_ID)
WHERE I.SEX_UPON_INTAKE LIKE '%INTACT%'          # '%aa%'
AND O.SEX_UPON_OUTCOME REGEXP 'SPAYED|Neutered'  # 정규표현식